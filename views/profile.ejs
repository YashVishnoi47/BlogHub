<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/output.css">
</head>

<body class="select-none">

    <div class="min-h-screen flex flex-col items-center ">
        <!-- Profile Card -->
        <div class="bg-white shadow-lg rounded-lg p-6 mt-8 w-full max-w-3xl relative">
            <div class="flex items-center">
                <!-- Profile Picture -->
                <img src="/images/profile-icon.jpg" alt="Profile Picture"
                    class="w-20 overflow-hidden h-20 rounded-full object-cover border-2 border-blue-500">
                <div class="ml-4">
                    <!-- User Info -->
                    <h2 class="text-2xl capitalize font-bold text-gray-800">
                        <%= user.name %>
                    </h2>
                    <p class="text-gray-600">
                        <%= user.email %>
                    </p>
                </div>

                <div class="btns absolute right-12 flex gap-2 ">
                    <a class="border-2 border-black flex justify-center items-center p-1" href="/user/logoutUser">logout</a>
                    <a class="border-2 border-black flex justify-center items-center p-1" href="">Add blog</a>
                </div>
            </div>
        </div>


        <!-- Blogs Section -->
        <div class="bg-white shadow-lg rounded-lg p-6 w-full max-w-3xl">
            <h3 class="text-xl font-bold text-gray-800 mb-4">Your Blogs</h3>
            <% if (blogs.length> 0) { %>
                <ul class="space-y-4">
                    <% blogs.forEach(blog=> { %>
                        <li class="border-b pb-4">
                            <h4 class="text-xl font-bold text-gray-800">
                                <%= blog.title %>
                            </h4>
                            <p class="text-gray-600 text-sm mt-1">
                                <%= blog.blogText %>
                            </p>
                            <!-- <a href="/blogs/<%= blog._id %>" class="text-blue-500 text-sm mt-2 inline-block">Read More</a> -->
                        </li>
                        <% }) %>
                </ul>
                <% } else { %>
                    <p class="text-gray-600">You haven't created any blogs yet.</p>
                    <% } %>
        </div>


    </div>

    </div>
</body>

</html>